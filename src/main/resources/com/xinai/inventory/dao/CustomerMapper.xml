<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinai.inventory.dao.SalesDao">
    <resultMap id="customerMap" type="Customer">
        <id property="customerId" column="customer_id"/>
        <result property="customerName" column="customer_name"/>
        <result property="contactPerson" column="contact_person"/>
        <result property="contactPhone" column="contact_phone"/>
        <result property="cooperationStatus" column="cooperation_status"/>
        <result property="address" column="address"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="getCustomerList" resultMap="customerMap">
        SELECT * FROM customer
        <where>
            <if test="customerName != null and customerName != ''">AND customer_name LIKE concat('%',#{customerName},'%')</if>
            <if test="cooperationStatus != null and cooperationStatus != ''">AND cooperation_status = #{cooperationStatus}</if>
        </where>
        ORDER BY create_time DESC
        <if test="offset != null and limit != null">LIMIT #{offset}, #{limit}</if>
    </select>

    <select id="getCustomerCount" resultType="int">
        SELECT count(*) FROM customer
        <where>
            <if test="customerName != null and customerName != ''">AND customer_name LIKE concat('%',#{customerName},'%')</if>
            <if test="cooperationStatus != null and cooperationStatus != ''">AND cooperation_status = #{cooperationStatus}</if>
        </where>
    </select>

    <select id="getCustomerById" resultMap="customerMap">
        SELECT * FROM customer WHERE customer_id = #{customerId}
    </select>

    <insert id="insertCustomer" parameterType="Customer" useGeneratedKeys="true" keyProperty="customerId">
        INSERT INTO customer (customer_name, contact_person, contact_phone, cooperation_status, address, remark)
        VALUES (#{customerName}, #{contactPerson}, #{contactPhone}, '0', #{address}, #{remark})
    </insert>

    <update id="updateCustomer" parameterType="Customer">
        UPDATE customer SET customer_name = #{customerName}, contact_person = #{contactPerson},
            contact_phone = #{contactPhone}, cooperation_status = #{cooperationStatus},
            address = #{address}, remark = #{remark}
        WHERE customer_id = #{customerId}
    </update>

    <select id="getAllActiveCustomers" resultMap="customerMap">
        SELECT * FROM customer WHERE cooperation_status = '0' ORDER BY customer_name
    </select>
</mapper>
